import { ScreenshotScene } from "../components/ScreenshotScene";

const Passkeys = () => (
  <ScreenshotScene
    src="screens/sign-in.png"
    headline="No passwords. Ever."
    highlight="Ever."
    caption="Face ID, Touch ID, or your device's own security. Nothing to leak."
  />
);

export { Passkeys };
