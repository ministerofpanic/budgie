import { ScreenshotScene } from "../components/ScreenshotScene";

const Reports = () => (
  <ScreenshotScene
    src="screens/reports.png"
    headline="See exactly where it goes."
    highlight="goes."
    caption="Spending by category, net worth over time, and Age of Money - a real measure of financial cushion."
  />
);

export { Reports };
