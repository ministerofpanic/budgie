# Active

One item at a time. Finish it completely before pulling the next from `BACKLOG.md`.

---

Nothing active. Pull the next item from `BACKLOG.md` when there's a next
phase to define - "Later, not v1" items need scoping into an active phase
first.
